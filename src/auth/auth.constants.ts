/** Six-digit email verification / password-reset OTP length. */
export const OTP_LENGTH = 6;

/** UI resend cooldown before the link becomes clickable again (2 minutes). */
export const OTP_RESEND_COOLDOWN_SECONDS = 120;
